# خريطة الكورس

## الفكرة العامة

الموقع برنامج يعمل غالبًا داخل المتصفح، لكنه يعتمد على أجزاء أخرى مثل الخادم وقاعدة البيانات وخدمة أسماء النطاقات

ستمر في هذا الكورس عبر المسار التالي

```text
المستخدم ← المتصفح ← DNS ← الخادم ← قاعدة البيانات أو الملفات
                   ↓
              HTML + CSS + JavaScript
```

## ماذا تحتاج

يكفيك محرر نصوص ومتصفح حديث

للتجارب المتقدمة ستحتاج إلى Node.js أو Python، لكن معظم أمثلة البداية تعمل بفتح الملف مباشرة

## خطة مقترحة

اقرأ قسمًا واحدًا في كل جلسة، ثم نفذ المثال بيدك وعدّل قيمة واحدة على الأقل

لا تنتقل من موضوع إلى آخر قبل أن تستطيع شرح الفكرة بكلماتك

## ناتج الكورس

في النهاية ستبني صفحة مهام صغيرة فيها نموذج إدخال وقائمة مهام وتخزين محلي داخل المتصفح
